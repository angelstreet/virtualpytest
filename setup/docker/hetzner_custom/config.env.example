# VirtualPyTest Hetzner Custom - Dynamic Configuration
# This file controls how many backend hosts to deploy

# Number of backend hosts (1-10)
# Based on 3.8GB RAM: Recommend max 6 hosts
# Each host uses ~300-400MB RAM
HOST_MAX=4

# Starting port for hosts (incremental: 6109, 6110, 6111, ...)
HOST_START_PORT=6109

# Domain for external access
DOMAIN=api.virtualpytest.com

# Server port
SERVER_PORT=5109

# Grafana Configuration (optional)
# Set to true to deploy Grafana monitoring dashboard
ENABLE_GRAFANA=false
GRAFANA_PORT=3000

# VPN Configuration (optional)
# If enabled, requires /etc/wireguard/wg0.conf
# Get free config: protonvpn.com → Downloads → WireGuard → Download .conf
# Install: sudo apt install wireguard && sudo cp your.conf /etc/wireguard/wg0.conf
USE_VPN=false
