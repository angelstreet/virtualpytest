# VirtualPyTest Proxmox VM Configuration

# ============================================
# VM SPECIFICATIONS
# ============================================

# VM ID (100-999, must be unique on Proxmox host)
VM_ID=100

# VM Name
VM_NAME="virtualpytest"

# RAM in MB (4096 = 4GB, matches Hetzner CX23)
# Recommended: 4GB for 8 hosts, 8GB for 16 hosts
VM_MEMORY=4096

# CPU cores (2 cores = CX23 equivalent)
VM_CORES=2

# Disk size (40G = CX23 equivalent)
VM_DISK_SIZE=40G

# ============================================
# NETWORK CONFIGURATION
# ============================================

# Proxmox bridge interface (default: vmbr0)
# Check with: ip link show | grep vmbr
VM_BRIDGE="vmbr0"

# Network configuration (DHCP or static IP)
# DHCP:
VM_IP_CONFIG="ip=dhcp"
# Static IP example:
# VM_IP_CONFIG="ip=192.168.1.100/24,gw=192.168.1.1"

# DNS nameserver (optional, defaults to gateway)
VM_NAMESERVER="8.8.8.8"

# ============================================
# CLOUD-INIT CONFIGURATION
# ============================================

# Default user (ubuntu recommended)
VM_USER="ubuntu"

# Password for VM user (leave empty to use SSH key only - more secure)
VM_PASSWORD=""

# Path to SSH public key for passwordless login (recommended)
# Example: VM_SSH_KEY="$HOME/.ssh/id_rsa.pub"
VM_SSH_KEY=""

# ============================================
# DOMAIN & SSL
# ============================================

# Your domain name with SSL certificate
# This should point to Proxmox host's public IP
DOMAIN="virtualpytest.example.com"

# ============================================
# STORAGE
# ============================================

# Proxmox storage location
# Common values: local-lvm, local, local-zfs, ceph
# Check with: pvesm status
STORAGE="local-lvm"

# ============================================
# ADVANCED OPTIONS
# ============================================

# Start VM on boot
VM_ONBOOT=1

# Enable QEMU guest agent (recommended for better VM management)
VM_AGENT=1

# CPU type (host = best performance, x86-64-v2-AES = portable)
VM_CPU_TYPE="host"

# Memory balloon (allow VM to return unused RAM to host)
# Min RAM in MB (2048 = 2GB minimum, can shrink from 4GB)
# Set to 0 to disable ballooning
VM_BALLOON=0

# ============================================
# USB DEVICE PASSTHROUGH (Optional)
# ============================================

# Pass USB devices to VM for testing (phones, STBs, IR blasters)
# Find devices with: lsusb
# Example: USB_DEVICE_1="host=04e8:6860"  # Samsung phone by vendor:product ID
# Or pass entire USB port: USB_DEVICE_1="host=1-2"
USB_DEVICE_1=""
USB_DEVICE_2=""

# ============================================
# GPU PASSTHROUGH (Optional)
# ============================================

# Pass GPU to VM for browser hardware acceleration
# Requires IOMMU enabled in BIOS
# Find GPU with: lspci | grep VGA
# Example: GPU_PASSTHROUGH="01:00"
GPU_PASSTHROUGH=""

# ============================================
# UBUNTU CLOUD IMAGE
# ============================================

# Ubuntu version (22.04 LTS recommended)
UBUNTU_VERSION="22.04"

# Cloud image URL (auto-constructed, don't change unless needed)
UBUNTU_IMAGE_URL="https://cloud-images.ubuntu.com/jammy/current/jammy-server-cloudimg-amd64.img"

